# This file marks the `app` folder as a Python package.
# Without it, Python wouldn't recognize `app` as an importable module.
# Think of it as a "table of contents" that says: "this folder is a module."
